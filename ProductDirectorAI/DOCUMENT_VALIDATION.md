# 文档与合同检查记录

检查日期：2026-09-10。

## 本次换电脑交接校验

文档收尾由用户指定的 SOL 5.6 执行，主代理负责审阅、验证与 GitHub 同步。本轮仅更正文档、补充交接和加强项目忽略规则，没有修改业务实现。

| 项目 | 本次实际结果与边界 |
| --- | --- |
| 文档扫描 | 18 份 Markdown；围栏均闭合 |
| 本地相对文件链接 | 41 个，全部目标存在；不包含外网可用性验证 |
| JSON | 7 段 JSON 围栏及扫描到的 JSON 文件均可解析 |
| 主规划 | 18 个有序主章节；V1–V6 六期章节均保留 |
| 起始合同 | PowerShell Test-Json 对示例与 Schema 校验为 True；不等于当前 API 全量实现 |
| 敏感信息 | 40 个文本文件模式扫描未命中常见 API key、GitHub token 或私钥；这不是对所有类型秘密的数学保证 |
| 截图审查 | 既有 MiniMax 设置证据图未显示明文密钥；本轮原始登录、云控制台及聊天截图全部未纳入包 |
| 前端构建 | Vite build PASS，4574 modules；仍有大于 500kB bundle 警告 |
| Sites 打包 | prepare-sites-build PASS |
| Worker 回归 | 4/4 PASS，无跳过 |
| Python | compileall PASS；本机 Python 3.12.14 |
| 本机 API | 原 8000 服务未运行；临时启动 127.0.0.1:18001 后 /api/v1/health 返回 HTTP 200、status=ok，Blender/FFmpeg available；测试后终止临时进程 |
| 版本读数 | 本机 Blender 5.2.1 LTS、Node v24.19.0；不代表云端或新电脑安装版本 |
| 本次未运行 | 新图片/GLB 渲染、GPU 云渲染、浏览器视觉复验、MiniMax/ComfyUI/发布、数据迁移 |

交付包包含该项目受版本管理的源码、全部文档、合同、参考图与通用夹具，不包含 ZIP 自身、var、数据库、依赖、私人素材或凭证。打包程序逐项比较归档成员与源文件的 SHA256，并检查排除项；GitHub 同步以最终成功推送及远端提交校验为准。

## 历史合同检查（原始记录）

本记录检查交付文档与起始合同，不代表 ProductDirectorAI 软件已实现、Blender 已出片、Provider 已连通或 GitHub 已同步。

| 检查 | 结果 |
| --- | --- |
| 完整规划结构 | 18 个主章节、6 个版本 |
| 施工任务 | 55 个带编号任务，含 V1 内部 P0 前置门 |
| 六期覆盖 | 均包含目标、界面/流程、数据/API、Provider、本地/云、状态、施工、测试、禁止项与退出条件 |
| Markdown 围栏 | 32 个围栏标记，成对闭合 |
| 主规划内 JSON 示例 | 6 段全部可以解析 |
| 目录锚点 | 全部可对应实际标题 |
| 文档相对链接 | 检查时的 7 个文件链接均存在 |
| V1 计划示例 | 通过提供的 JSON Schema 校验 |
| 尺寸错误负例 | 540×1920 被拒绝 |
| 额外脚本字段负例 | 未定义 script 字段被拒绝 |
| 镜头数量负例 | 只有一个 Shot 被拒绝 |
| 时间一致性 | 示例 3×48=144 帧，24fps，6 秒 |
| 参考图 | 原始上传图完整保存在 assets/ 下 |

结构校验以 PowerShell 的 JSON 解析与 Test-Json 执行。人工核对另外修正了 V1 Strict 请求与 V3 认证的区别、3D/图片预演的不同合同、相机角度和帧采样定义，以及批次计划与产品版本的相容性规则。

后续产品开发仍需按每期验收要求执行真实环境、数据迁移、渲染、模型、恢复、界面和发布检查。本次没有重新运行这些完整链路；历史局部结果见原报告并受其证据范围限制，当前完整 V1 为 PARTIAL。
