# 图片转 3D 与场景生成修复记录

## 用户问题

上传图片后仍显示「图片 · 2D」，没有重建入口；视频沿用基础镜头，没有执行分段场景描述，几何模型缺少原图颜色材质。

## 已实现，尚未部署

- 图片卡片下新增裁切选择、生成 3D、任务进度、错误提示和模型回收后自动选择入口。上传与重建是两步操作，多视图图片需框选单个完整产品。
- 后端校验裁切边界；场景计划解析明确的连续时间段，保留各段描述，使用原始彩色产品图作为生成参考。
- 场景生成保留供应任务标识和来源哈希；输出标记待视觉审核，不冒充严格保真成片。当前场景路径为无声预览，几何重建不承诺纹理恢复。

## 已有证据

- 紫色修剪器图片后台真实重建成功，返回约 12 MB GLB，资产 ID `21c15971-e59b-4c16-bcbc-855a6fa25014`。这只验证后台转换，不能视作用户页面已修复；也未验证最新机器人图片。
- 隔离云工作目录新增 API/解析测试 8 项通过。
- 本地最新前端生产构建通过，前端逻辑与 Sites 测试合计 13/13 通过；git diff --check 通过。

## 阻塞与未验收项

云端全量后端测试与五场景真实生成启动后 SSH 连接被远端关闭。后续三次连接均在握手阶段返回 `Connection closed by 117.50.44.60 port 22`，尚不能确定服务器或任务最终状态。本地 `http://127.0.0.1:4173/` 连接被拒绝。

因此：全量测试结果未知、真实五场景视频结果未知、生产部署未执行、用户页面端到端未验收。不得宣称全部 PASS。

恢复服务器连接后先取回 `/home/ubuntu/pd-scene-fix-tests.log` 与 `/home/ubuntu/pd-scene-real-validation`，核对生产仓库和运行任务，避免重复提交 GPU 作业。随后部署、恢复页面连接，并对最新上传图片执行裁切→重建→模型选择→预览验收，记录截图和真实产物证据。

## 连接恢复及复验

本地 TUN 接管服务器流量，同时本机直连出口已变化，未命中 SSH 单 IP 白名单。已添加该服务器的本地直连路由，并为当前出口补充 TCP 22 单 IP 规则；SSH 恢复，重新启动仅监听回环的 4173 隧道。未重启云实例。

取回全量测试结果：654 项首次运行有 2 个失败、12 个错误，均在 H3 Provider 测试中；旧测试给 64×64 图片传了 256×256 裁切，新边界校验正确拒绝。修正测试输入为 64×64 后，该模块 27/27 通过；其余模块首次运行通过。新增场景测试 8/8 通过。

真实五场景生成已完成：1080×1920、24 fps、360 帧、15 秒。抽帧确认五段包含人物放置、投影、手持配件、手机、产品展示，保留原图彩色外观。生成的人物互动与产品细节仍可能偏离参考，不作为严格保真或广告发布验收。该样片由真实生成模块直接验证，尚不是部署后网页 Run 全链路的证据。

## 已部署与机器人图片端到端验收

代码 `7ad1605` 已推送 GitHub main 并部署；`93eb118` 修正 GLB 预览 Y 轴向上、相机从上方观察、居中缩放与实际场景数显示。云端再次生产构建、前端 13 项测试通过，API/Web active，本地页面恢复访问。

通过用户原工作台实际操作：机器人图片框选 `[0,0,875,1121]` → 点击「生成 3D 模型」→ 页面显示真实任务进度 → 模型入库并自动切换 → WebGL 显示可辨认的机器人几何模型。真实任务约 55 秒完成，GLB 约 8.1 MB。用户无需再次上传该图片。

- Provider job：`fd4c880f-d4a0-4651-97dd-21019aebe35d`，SUCCEEDED。
- 模型资产：`8f3c4240-d244-4d7b-a493-3e1b9b414d72`。
- GLB SHA-256：`76d81665e37101b3943d7ab984a18e96c85b35817488b1e4e3fda9a8e3976de3`。
- 浏览器复核：新版本刷新后模型正向显示；GLB 几何为灰色，未声称恢复纹理材质。

另从此模型的原图来源，在页面创建并启动两场景 6 秒视频：客厅地板行驶、书桌暖光展示。Run 对应 job `3d104b19-fb8f-447a-8231-fadc0c94c9c9` 已完成，状态 VERIFICATION_PASSED / SCENES_REVIEW，媒体 QA passed=true。输出 H3_SCENES、1080×1920、24 fps、144 帧、6.0 秒；两段各 72 帧，分别记录独立 Provider ID、提示词与视频哈希。Manifest 如实记录 GENERATIVE / NOT_VERIFIED。

抽帧可确认两种指定环境、白橙色产品外观；书桌段生成了额外线缆且产品放在桌面，仍需视觉审核，不能标为提示词细节全部符合。五场景样片与机器人样片均是真实 H3 视频，不是 GLB 灰模合成或静态图替代。

追加 `5b35b60`：点击历史场景任务时恢复不可 PATCH 的场景计划、原描述、产品及输出规格，避免刷新后显示默认三镜头造成误解。云端生产构建及前端 13 项测试再次通过。
