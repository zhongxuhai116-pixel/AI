# V4-04/05：人物能力校验与真实人物遮挡合成（2026-09-13，云端）

结论：**人物路线能力校验已落地并如实报告；真实人物遮挡合成闭环（Proxy 人物层作为遮挡层盖回，可见区域保真 + 豁免计数 + 双产品零误报）。** 产物在云端 `/home/ubuntu/pd-v307-calib-20260913/composite_v405/`（不入 Git）。后端 **370/370 OK**（云端 292.3s）。

## 1. V4-04 人物能力校验

- 人物创建的 `asset_refs` 现在**校验素材存在且属于同一 Owner**（404/403，与产品审核证据同规则）；
- `GET /api/v1/characters/{id}/capability` 如实报告：
  - `proxy_previz`：AVAILABLE（确定性人体 Proxy 可用于尺度/路径/遮挡与接触预演）；
  - `generation_route`：**NOT_CONFIGURED**——未配置经批准的真人感人物生成工作流，接入前不得宣称可用（不伪造能力）；
  - `licensed_assets`：逐引用 BOUND/MISSING 状态；`usable_for_final_person_layer` 仅当授权素材来源 + 素材绑定 + 许可/同意记录齐备时为 true（合成 Proxy 来源为 false）。
- 测试：`test_character_asset_refs_validated_and_capability`（8/8 V4 合同测试）。

## 2. V4-05 真实人物遮挡合成（person layer = V4-02/03 Proxy 帧）

输入：V3-07 校准 CC0 渲染 72 帧（product/mask + shadow/reflection 层）+ H3 真实背景 + Proxy `proxy_only` 帧（1–60，交互窗口 10–60 声明为必需层）。

| 指标 | 结果 |
| --- | --- |
| composite passed / pixel_lock_ok | **true / true**（72 帧） |
| approved display 产品对照 | **PASS**（未遮挡掩码区与可信产品一致） |
| occlusion 豁免计数 | **4,554,420 像素**（人物真实遮挡产品，逐帧计数） |
| 帧 46 数值复核 | 遮挡区内像素 60,272 / 未遮挡锁定像素 34,918；锁定区 composite↔产品差 **0.0009**（≈0.23/255）；遮挡区 composite↔Proxy 差 **0.0004** |
| 双产品检测 | **72/72 PASS，零误报**（人物层未被误判为多余产品） |

语义说明：人物层按 V3-05 遮挡合同盖回（front-depth-priority），产品掩码内被人物覆盖的像素从像素锁定中豁免并计数，其余掩码区保持像素锁定——可见区域保真由锁定断言保证；接触质量由 V4-02/03 几何 QA 报告覆盖（正例偏差 1 帧/穿透 4mm）。

## 3. 范围说明（如实记录）

1. 人物层是**确定性 Proxy**（圆柱/球体），不是真人感人物——真人感人物层需要经批准的生成工作流或授权素材（V4-04 能力报告如实为 NOT_CONFIGURED）。
2. 预演/合成帧的**人工目检**未完成（本环境无图像查看）；以逐像素数值校验与逐帧轨迹 JSON 佐证。
3. 击打（single_punch_target）与双人非同时动作未验证（属后续轮次）。
