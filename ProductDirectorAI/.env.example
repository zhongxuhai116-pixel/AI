# setup.ps1 generates independent random keys. Never commit .env.
# Plain KEY=value syntax; do not add shell expressions or quotes.
PRODUCTDIRECTOR_OWNER_TOKEN=
PRODUCTDIRECTOR_WORKER_TOKEN=
PRODUCTDIRECTOR_COMFYUI_URL=http://127.0.0.1:8188
# PostgreSQL is optional; default local installation uses SQLite.
# PRODUCTDIRECTOR_DATABASE_URL=postgresql://USER:PASSWORD@localhost/DB
